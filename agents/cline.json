{
  "id": "cline",
  "name": "Cline",
  "version": "3.0.65",
  "profileId": "cline",
  "dist": {
    "kind": "npx",
    "package": "cline@3.0.65",
    "args": [
      "--acp"
    ]
  },
  "storage": {
    "mode": "files",
    "env": {
      "HOME": "${RUNTIME_DIR}"
    },
    "artifacts": [
      {
        "durable": ".credentials.json",
        "runtime": ".credentials.json",
        "sync": "exit",
        "when": [
          "self"
        ]
      }
    ]
  },
  "auth": {
    "modes": [
      "self",
      "api_key"
    ]
  }
}
